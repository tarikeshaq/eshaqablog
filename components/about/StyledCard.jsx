import Card from 'react-bootstrap/Card';
import styled from 'styled-components';

export const TransparentCard = styled(Card)`
    background-color: transparent!important;
`;

export const CenteredHeader = styled(Card.Header)`
    text-align: center;
`;